const REQUIRED = 'Campo obrigatório';

const NAME = 'O nome informado é muito curto para continuar';

const EMAIL = 'Por favor, insira um email válido';

const CEP = 'O CEP deve seguir o formato: NNNNN-NNN';

const CPF = 'O CPF deve seguir o formato: NNN.NNN.NNN-NN';

const RG = 'Escreva o RG conforme o documento';

const BIRTH = 'A data de nascimento deve seguir o formato: DD/MM/AAAA';

const INVALID_BIRTH = 'A data de nascimento informada é inválida';

const PHONE = 'O celular deve seguir o formato: (NN) NNNNN-NNNN';

const CITY = 'A cidade informada não pode possuir menos de três letras';

const UF = 'A sigla do estado deve possuir apenas duas letras';

const NEIGHBORHOOD = 'O bairro informado é muito curto para continuar';

const STREET = 'A rua informada não pode possuir menos de três letras';

const NUMBER = 'O número deve possuir apenas caracteres numéricos';

const ADDRESSDETAILS = 'O complemento não pode possuir menos de três letras';

const DUPLICATE = 'Valores duplicados não são permitidos';

export const msg_errors = {
    REQUIRED,
    NAME,
    EMAIL,
    CEP,
    CPF,
    RG,
    BIRTH,
    INVALID_BIRTH,
    PHONE,
    CITY,
    UF,
    NEIGHBORHOOD,
    STREET,
    NUMBER,
    ADDRESSDETAILS,
    DUPLICATE,
};